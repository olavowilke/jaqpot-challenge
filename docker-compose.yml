services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: jaqpot
    ports:
      - "5432:5432"
    volumes:
      - jaqpot_pg:/var/lib/postgresql/data
volumes:
  jaqpot_pg:
